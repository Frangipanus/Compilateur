fun fib(n: int)
  var x := 0
  var y := 1
  repeat(n)
    val y0 = y
    y := x+y
    x := y0
  x

fun main()
  var x := 0
  var y := 1
  repeat(5)
    val y0 = y
    y := x+y
    x := y0
    println(x)
  x